# DESIGN-DECISIONS — 现系统全部重要设计决策清单（审视靶子，2026-09-16）

> 用途：地毯式审视的对象。每条=决策+理由/证据+已知弱点。审视席（架构/用户价值/红队重构）逐条给 保持/优化/替换/重构 判决。
> 用户目标锚（审视的最终判据）：**让科学 Agent 具有强大的文献搜索、检索、理解能力，为科学问题提出与具体研究过程提供文献信息与领域认知，快速准确拿到实际研究相关信息。**
> 状态注：本清单描述"现在是什么"，不辩护"必须永远是什么"——任何一条都可被判死刑。

## A. 方法论层

**A1 访问负载驱动设计**（表示的设计输入=消费者访问负载，非内容形态）
理由：09-03 转向定稿；E2 失效地图（聚合 2.33 最差、强模型修不好）+负载目录（关系型查询 ~30%、压缩第一成本）。弱点：负载目录主体是人类查询（Asta），agent 侧只有代码行为挖掘非查询日志（v6 R1-F1/R3-F4/DA C-C 三面攻击：循环性+分布迁移+负载漂移无故事）。

**A2 预编译 eager 优先，非查询时 lazy 组装**
理由：与 KG-RAG 的立论分界线；摊销经济学（Lacuna 4× 先例）；编译一次查询多次。弱点：DA 指为"全篇最大未审视分岔"——从未论证 eager over lazy；查询重尾分布下 lazy+缓存可能更优；CRITICAL①"编译 vs 缓存不可区分"未全闭（缺预算配对臂）。

**A3 不做图本体**（图=可选序列化细节）
理由：用户 09-03 裁定"内容表达力是错误差异化轴，图灵完备表示间无表达力差异，只有一等公民与访问路径差异"。弱点：citation-graph 在 Type B 检索的实证价值（contest/SPAR/MDAQA 都证实引用结构有用）我们只在血缘视图局部利用；"弃图"与"图降维使用"的边界没系统论证过。

## B. 记录层

**B1 类型化记录（7 类）非文本块非三元组**
理由：负载对齐（配置/数值/取代/条件四轴=SciAtlas 自认空白）；每条=可审计最小事实。弱点：schema 表达力边界=抽取天花板（公式/图内容/长 verbatim 都在协议外，AirQA 4-5 题结构性弃分实证）。

**B2 冻结 schema+仲裁式版本演进（v1.0→v1.4，非自演化）**
理由：Stage A 自演化完整验证失败（采纳 0/5、novel 边 6runs 全 0、LLM 方差主导）；C 线查新"冻结+仲裁+canary"最接近真空；可审计性=信任柱。弱点：跨域扩展需人工重建 vocab（AirQA 实操过）；仲裁债积压 2109+entity_queue 9551=治理吞吐是真瓶颈；负载漂移无自动响应（用户 09-16 提议"两速演化"待议）。

**B3 quote-first 逐字锚**（先抄原句再从抄件填字段，数值必须逐字在 quote 中）
理由：编造 0/178 的机制来源；后检可确定性校验。弱点：牺牲抽象/综合型知识（长 verbatim 超协议、跨句综合无法逐字锚）；quote≤40 词限制复杂事实。

**B4 epistemic 三态**（stated/demonstrated/cited）
理由：v1.4 仲裁实证（最大违规类=模型把转引塞进 strength）；信任分层。弱点：三态粒度够不够（置信度/争议度未表达）；抽取端模型自判 epistemic 的准确率未单独测过。

**B5 维度坐标**（subject/setup/budget/variant/hyperparam，条件性一等公民）
理由：67% 主张带条件实测；matrix 可比性分带依赖它。弱点：单层表头 56% 残留（维度落位失败主因，F35 对症未上线）；维度词表每域人工重建。

**B6 absence 三态+derived absence**（缺失性可表达）
理由：覆盖/gap 查询是 agent 真实需求（新颖性检查）；RAG 结构性做不到。弱点：F32 实证 derived absence 曾被可达性伪影污染 321→8（危险类别，需持续审计）；CROWN-QA/UQS 已占认识论与工件形态位。

## C. 建库管线

**C1 确定性三明治**（LLM 只做语义判断：skeleton/slot/round2；结构任务全确定性：postcheck/table/views/F13）
理由：规则只用于结构/确定性任务铁律；LLM 方差教训。弱点：确定性与语义的边界是人工划的，F33（空格敏感）类缺陷说明 parser 脆性；边界划错=56% 残留类盲区。

**C2 表格走确定性 parser 非 LLM 转写**
理由：F24 洞察（表格是结构非语义）；成本与保真双优。弱点：复杂表（单层表头/转置/嵌套）parser 覆盖不全→F35 补丁层排队。

**C3 canary 押运+postcheck 门**（金丝雀混批、掉批即停；修复/丢弃有台账）
理由：过程质量守卫非事后验证；建库门全程 7/7+0 记录。弱点：canary 覆盖的记录形态有限（新形态缺陷靠 postcheck+人工抽读兜底，实读双包补跑仍欠账）。

**C4 registry 治理**（实体生长 LLM+F34 确定性守卫；仲裁债显式记账）
理由："+"变体归并吃 gold 的活体教训；LLM 合并不可独信。弱点：唯一匹配才链接=保守（融合行头留空）；跨篇实体对齐仍是已知未解难题（DA C-E"半可信死区"警告）。

**C5 小模型建库**（Qwen3.6-27B ~280k tok/篇）
理由：经济性=一等设计目标；PS-53 实测 15.78M/52 篇质量未塌（first_pass 0.921、gold16 重抽与旧库几乎重合）。弱点：抽取质量上限低于大模型（e2-r2 抽取等级效应：ds 抽 2.90→Qwen 抽 3.50 视图消费分）；量化/更小模型未验证。

## D. 视图与访问层

**D1 视图=答案结构物化**（四张：matrix/genealogy/coverage/cards，非通用摘要）
理由：负载类型化（对比/时序/缺口/档案四类场景）；GraphRAG community summaries 反例划界。弱点：四张=预设负载假设，未覆盖的查询形态只能落 search 兜底（"两速演化"讨论的动因）；视图间无组合代数（v6 m-A"六个 ER 图事后升格"批评未被完全回应）。

**D2 matrix 可比性分带**（budget/条件不同不进同格）+derived 分离标注
理由：不做不公平对比是决策支持的第一美德；派生与记录永不混。弱点：分带依据的维度质量（B5 弱点传导）；带间比较需求（跨 budget 趋势）无表达。

**D3 typed tools+search 兜底**（API 即知识模型；召回红线 D2：结构化不牺牲召回）
理由：Lacuna 0.028 警训；工具面=负载操作化。弱点：工具集静态（无负载驱动生长机制）；NAV 提示是补丁非通解；search 兜底质量依赖 embedding（纯向量输词面的教训在 KB 内同样可能成立）。

## E. 答题端（冻结态）

**E1 ReAct+有损笔记+机械锚门+F31V2 机械转写**（hybrid，判决冻结停止加补丁）
理由：harness 调研 10 系统唯一组合；有损笔记直接税仅 2/16；补丁堆=对模型行为不可靠的对冲=agentic 固有成本（09-17 判决：非设计病不重构）。弱点：演化产物非重新设计（Stage A 血统+补丁层）；风格维恒亏（流畅 −0.5~−0.7）未解；F28 收口参数新域过激史。

**E2 判分方法论**（官方 rubric 逐字+异厂 judge 替换+多 judge κ+编造独立红线）
理由：GPT-5 下架被迫替换；κ=0.39 臂级排序稳健先例。弱点：无官方校准锚；风格维判分偏置对我方系统性不利（PS 悲剧）。

## F. 未建成/缺口（诚实账）

**F1 Type B 海量外部检索未集成**（只有通道消融原型：query-decomp 最大杠杆/纯向量输词面/引用通道小杠杆）——系统范式是"有界语料预编译"，无廉价海量前端；所有大规模外部基准都考 Type B。
**F2 公式不产记录**（AirQA 公式题材料面全灭）；**F3 图内容未集成**（figure_channel 已建排队）；**F4 单层表头 56% 残留**（F35 排队）；**F5 负载遥测未标准化**（信号散落：usage_audit/NAV/auto_rows/PROJ_LEDGER，无统一 workload ledger）；**F6 计算型派生值无通道**（gold 需推算时结构性弃分）。

## G. 实验方法层

**G1 gold-blind/预注册冻结/帽+停线/一锤子**；**G2 仪器先审**（四次救场：BM25 假 0/GLM-4.6V 读值全错/E5 v1 三重缺陷/PS-53 操纵强度缺陷）；**G3 官方数字永不进对比表**；**G4 主场自建 gold 的循环性未全闭**（出题人=我方；外部基准部分缓解）。
