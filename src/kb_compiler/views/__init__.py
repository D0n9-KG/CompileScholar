"""Views layer (Stage B blueprint block 2): compile checked records into the
four views + narrative layer.

Consumers: typed tools (block 3), evaluation arms (single-doc rendering),
usage audit (governance subtraction channel).

Discipline: this layer CONSUMES records, never invents content. Every view
cell carries provenance (paper_id + record_id + quote); derived facts
(computed deltas, derived absences, comparability bands) are marked
provenance="derived" and kept separable from extracted facts (Stage A /
PaperScope lesson: derived-vs-extracted absence must not mix in rendering).
"""
